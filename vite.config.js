import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Vite is the tool that runs your React app locally and builds it for production.
export default defineConfig({
  plugins: [react()],
});


